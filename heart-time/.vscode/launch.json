{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run AI Main",
            "type": "debugpy",
            "request": "launch",

            "module": "ai.main",

            "cwd": "${workspaceFolder}",

            "console": "integratedTerminal"
        }
    ]
}